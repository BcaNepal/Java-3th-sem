import java.lang.Math;

public class MathDemo{
    float radius;

    public double calculateCirleArea(){
        double area = Math.PI * Math.pow(radius, 2) // radius * radius
        return area;
    }
}